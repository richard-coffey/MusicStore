@model IList<string>

@if (Model.Count > 0)
{
    <form asp-action="QuickEdit" method="post">
        @for (var i = 0; i < Model.Count; i++)
        {
            <div class="form-group">
                <label>Album name @(i + 1):</label>
                <input id="AlbumNames" name="AlbumNames" asp-for="@Model[i]" class="form-control" />
            </div>
        }
        <button type="submit" class="btn btn-primary">Update</button>
    </form>
}
else
{
    <h2>There aren't any Albums in the system</h2>
}